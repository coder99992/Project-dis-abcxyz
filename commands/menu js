const { SlashCommandBuilder, ActionRowBuilder, StringSelectMenuBuilder } = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('menu')
        .setDescription('Hiển thị menu chọn tùy chọn'),
    async execute(interaction) {
        const menu = new StringSelectMenuBuilder()
            .setCustomId('my_menu')
            .setPlaceholder('Chọn một tùy chọn...')
            .addOptions(
                { label: 'Tùy chọn 1', value: 'option_1' },
                { label: 'Tùy chọn 2', value: 'option_2' },
                { label: 'Tùy chọn 3', value: 'option_3' }
            );

        const row = new ActionRowBuilder().addComponents(menu);
        await interaction.reply({ content: '📋 Mời bạn chọn một tùy chọn:', components: [row] });
    }
};
